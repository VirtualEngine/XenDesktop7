<# VE_XD7StoreFrontStoreFarm\VE_XD7StoreFrontStoreFarm.Resources.psd1 #>
ConvertFrom-StringData @'
    UpdatingBaseUrl                      = Updating Citrix XenDesktop 7.x StoreFront base url '{0}'.
    RunningAddSTFAuthenicationService    = Running Add-STFAuthenicationService.
    CallingGetSTFStoreService            = Calling Get-STFStoreService for store '{0}'.
    RunningRemoveSTFStoreFarm            = Running Remove-STFStoreFarm.
    RunningSetSTFStoreService            = Running Set-STFStoreService.
    RunningSetSTFStoreFarm               = Running Set-STFStoreFarm.
    RunningNewSTFStoreFarm               = Running New-STFStoreFarm.
    RunningAddSTFStoreFarm               = Running Add-STFStoreFarm.
    RunningAddSTFStoreService            = Running Add-STFStoreService.
    RunningSetSTFStoreFarmLockedDown     = Running Set-STFStoreFarm for LockedDown setting.
    RunningRemoveSTFStoreService         = Running Remove-STFStoreService.
    RunningGetSTFAuthenticationService   = Running Get-STFAuthenticationService for virtual path '{0}'.
    RunningRemoveSTFAuthenicationService = Running Remove-STFAuthenicationService.
    SettingResourceProperty              = Setting resource property '{0}'.
    ResourcePropertyMismatch             = Property '{0}' is NOT in desired state; expected '{1}', actual '{2}'.
    ResourceInDesiredState               = Citrix XenDesktop 7.x StoreFront store is in the desired state.
    ResourceNotInDesiredState            = Citrix XenDesktop 7.x StoreFront store is NOT in the desired state.
'@
